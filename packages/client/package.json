{
  "name": "@mcc/client",
  "private": true,
  "version": "0.1.0",
  "description": "MyCamerasController camera viewer",
  "author": "MyCamerasController",
  "main": "dist/main.js",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "dev": "node build.mjs && electron .",
    "dist:win": "node build.mjs && electron-builder --win nsis"
  },
  "devDependencies": {
    "@mcc/shared": "*",
    "electron": "^32.1.0",
    "electron-builder": "^25.1.0",
    "esbuild": "^0.23.1"
  }
}
